(* rv32emu - RISC-V RV32I Emulator
   Main library module *)

let version = "0.1.0"

(* Re-export submodules *)
module Types = Types
module Bits = Bits
module Registers = Registers
module Memory = Memory
